# autocommit

Uses Claude Code to split uncommitted dotfiles changes into logical small commits.

## Usage

```bash
# Generate and preview commits.sh
bash autocommit/run.sh

# Review, then run
bash autocommit/commits.sh
```
